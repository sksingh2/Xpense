<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#B8F0C8"
    android:padding="10dp"
    tools:context=".ProfileFragment">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <ListView
        android:id="@+id/prof_list"
        android:layout_width="315dp"
        android:layout_height="500dp"
        android:layout_marginStart="40dp"
        android:layout_marginTop="130dp"
        android:background="@drawable/round_corner"
        android:divider="#B8F0C8"
        android:dividerHeight="2dp"
        android:paddingStart="15dp"
        android:paddingEnd="15dp" />

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarPersonal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/round_corner"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        android:backgroundTint="@color/white"
        app:title="Personal"
        app:titleMarginStart="30dp"
        app:logo="@drawable/ic_menu"
        />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/float_personal_add"
            android:contentDescription="@string/add_personal_items"
            android:layout_below="@+id/toolbarPersonal"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="10dp"
            android:src="@drawable/ic_add"
            android:layout_marginTop="10dp"/>

    </RelativeLayout>
</FrameLayout>